## NOTE

    if there is improper input in the ws data input , emit an error event to client. the frontend will listen and  display the error message

# TODO --->

    fire conversation event whenever there is a change in the direct/group convos also when authenticated ==> find a better way (the socket id is not found in the room many times)
    debug receive:message ==> the same user gets the message but the other person in same room does not
    produce message on send:message to message broker

## Frontend -->

## WS -->
