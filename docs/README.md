## NOTE

    if there is improper input in the ws data input , emit an error event to client. the frontend will listen and  display the error message

# TODO --->
    Create a class  store in that to hold the messages.
        create a variable store (array of objects)
        create a static function to check if the store exists and return it else create it (get Store)
        create functions to add to store and get from store
        add a function to clear the store and to get length

    create a function which will do a batch insert into the db
    if the insertion succeds , clear the store else push the data of the data into the kafka producer (maybe by create yet another producer for this)


## Backend -->

## Frontend -->

## WS -->
