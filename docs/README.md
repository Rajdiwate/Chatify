## NOTE

    if there is improper input in the ws data input , emit an error event to client. the frontend will listen and  display the error message

# TODO --->
    create a docker-compose
    make sure the redis and kafka both are active when the ws listens on send:message
    make displaying friendrequest realtime
    on friend request accept , the one who sent the request has to refresh

    structue well using env files n all

    Add group chat feature

    

